import type{EntityId,MaterialType,StageId}from'../matter/matterTypes';export type CompressionPhase='gathering'|'ready'|'levitating'|'aligning'|'compressing'|'impact'|'revealing'|'releasing'|'cooldown';export interface GridPosition{col:number;row:number}export type StageUnlockCondition={kind:'initial'}|{kind:'lifetime-material';stageId:StageId;material:MaterialType;count:number};export interface StageDefinition{id:StageId;order:number;name:string;gridPosition:GridPosition;inputMaterial:MaterialType|null;outputMaterial:MaterialType;implemented:boolean;unlockCondition:StageUnlockCondition;connectionTargets:StageId[]}export interface StageConnection{id:string;from:StageId;to:StageId;enabled:boolean;duration:number}export interface StageTransfer{id:string;entityId:EntityId;connectionId:string;progress:number}
